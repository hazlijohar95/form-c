{
  "name": "formc-studio",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace formc-web",
    "build": "npm run build --workspace @formc/engine && npm run build --workspace formc-web",
    "test": "npm run test --workspace @formc/engine && npm run test --workspace formc-web"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.7.0",
    "typescript": "^5.6.2",
    "vite": "^6.4.3",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
